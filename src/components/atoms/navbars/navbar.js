import Navbar from 'react-bootstrap/Navbar'

const NavBar = ({ as, bg, collapseOnSelect, expand, expanded, fixed, onSelect, onToggle, role, sticky, variant}) => <Navbar
    as={as}
    bg={bg}
    collapseOnSelect={collapseOnSelect}
    expand={expand}
    expanded={expanded}
    fixed={fixed}
    onSelect={onSelect}
    onToggle={onToggle}
    role={role}
    sticky={sticky}
    variant={variant}>
</Navbar>

export default NavBar